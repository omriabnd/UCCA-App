
(function () {
  'use strict';

  angular.module('zAdmin.pages.edit')
      .service('editService', editService);

  /** @ngInject */
  function editService(apiService) {
    var service = {
        
    }
    return service;
  }

})();
